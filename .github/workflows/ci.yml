name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov black isort mypy

    - name: Check code formatting with Black
      run: |
        black --check src/ tests/ --exclude '\.ipynb'

    - name: Check import sorting with isort
      run: |
        isort --check-only src/ tests/

    - name: Run Python syntax check
      run: |
        python -m py_compile src/simulators/*.py
        python -m py_compile src/batch/*.py
        python -m py_compile src/streaming/*.py
        python -m py_compile src/quality/*.py

    - name: Run unit tests
      run: |
        pytest tests/unit/ -v --tb=short || echo "No unit tests found"

    - name: Run integration tests
      run: |
        pytest tests/integration/ -v --tb=short || echo "No integration tests found"

  dbt-validation:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dbt
      run: |
        pip install dbt-duckdb

    - name: Install dbt packages
      run: |
        cd dbt_logistics
        dbt deps --profiles-dir .

    - name: Validate dbt models compile
      run: |
        cd dbt_logistics
        dbt compile --profiles-dir .

    - name: Run dbt model validation
      run: |
        cd dbt_logistics
        dbt parse --profiles-dir .

  docker-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Validate Docker Compose config
      run: |
        docker-compose -f infrastructure/docker-compose.yml config

  documentation:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Check README exists
      run: |
        test -f README.md

    - name: Check documentation exists
      run: |
        test -d docs/
        test -f docs/logistics_platform_blueprint_part1.md

    - name: Validate Makefile
      run: |
        make help
